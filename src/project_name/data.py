"""Data loading and validation. Replace stubs with project logic."""

from pathlib import Path


def load_raw(path: str | Path) -> object:
    """Load raw data. Returns project-specific structure (e.g. DataFrame)."""
    raise NotImplementedError
